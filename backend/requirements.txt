# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Orquestração + LLM + RAG
langgraph==0.2.60
langchain==0.3.13
langchain-core==0.3.28
langchain-google-genai==2.0.7
langchain-chroma==0.1.4
chromadb==0.5.23

# Observabilidade (LangSmith)
langsmith==0.2.3

# Avaliação (tabela bonita no terminal)
tabulate==0.9.0

# NOTE: o ecossistema LangChain evolui rápido. Se houver conflito de versão,
# rode `pip install -U langchain langchain-core langgraph langchain-google-genai langchain-chroma`
# e ajuste este arquivo. As versões acima formam um conjunto coerente testado na linha 0.3.x.
